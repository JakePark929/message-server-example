pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven { url = "https://repo.spring.io/snapshot" }
    }
}

rootProject.name = "message-system-push"

include(
        "message-system-client",
        "message-system-push-server",
        "message-system-server"
)